<UserControl x:Class="FamilyTree.Modules.Person.Views.InfoPersonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True"
             Height="400" Width="600" Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="7*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Image Grid.Row="0" Grid.Column="0" Grid.RowSpan="2"/>
        <TextBlock Grid.Row="2" Grid.Column="0">
            <TextBlock.Text>
                <MultiBinding StringFormat="{} {0} {1}">
                    <Binding Path="Person.FirstName"/>
                    <Binding Path="Person.LastName"/>
                </MultiBinding>
            </TextBlock.Text>
        </TextBlock>
        <Label Grid.Row="3" Grid.Column="0"
               Content="{Binding Person.Gender}"/>
        <Label Grid.Row="4" Grid.Column="0"
               Content="{Binding Person.DateOfBirth}"/>

        <Label Grid.Row="0" Grid.Column="1"
            Content="Események"/>
        <ScrollViewer Grid.Row="1" Grid.Column="1" Grid.RowSpan="4"
                      VerticalScrollBarVisibility="Auto">
            <ListView>

            </ListView>
        </ScrollViewer>
    </Grid>
</UserControl>
