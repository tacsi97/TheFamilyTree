<UserControl x:Class="FamilyTree.Modules.FamilyTree.Views.NewTreeDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True"
             Height="200" Width="200" Background="White">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0"
               Content="Családfa létrehozása" HorizontalContentAlignment="Center"
               FontSize="17" FontWeight="Bold"/>
        <Label Grid.Row="1"
               Margin="10"
               Content="Családfa neve"/>
        <TextBox Grid.Row="2"
                 Margin="10" Text="{Binding TreeName, Mode=OneWayToSource}"/>
        <Button Grid.Row="3"
                Margin="10"
                Content="Mentés" Command="{Binding SubmitCommand}"/>

    </Grid>
</UserControl>
