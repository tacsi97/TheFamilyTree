<UserControl x:Class="FamilyTree.Modules.FamilyTree.Views.DeleteFamilyTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"      
             Height="75" Width="200"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Szeretnéd töröli a következőt?"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Grid.Row="0"/>
        <TextBlock Text="{Binding FamilyTree.Name}"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Grid.Row="1"/>
        <Button Content="Törlöm" Command="{Binding DeleteCommand}"
                Grid.Row="2"/>
    </Grid>
</UserControl>
